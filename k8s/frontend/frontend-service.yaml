apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  namespace: estate
  labels:
    app: frontend
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    nodePort: 30080
    name: http
  selector:
    app: frontend
